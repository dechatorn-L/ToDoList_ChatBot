# 🚀 Skill Pipeline (สรุปเส้นทางการทำงานแบบด่วน)

แผ่นสรุปคำสั่งและลำดับการเรียกใช้ Skill ทั้ง 3 เส้นทาง ดูง่าย จบในหน้าเดียว

---

## 🟢 PATH 1: สร้างฟีเจอร์ใหม่ (New Feature)

ใช้เมื่อต้องการขึ้นระบบใหม่ หรือเพิ่มฟีเจอร์ตั้งแต่ศูนย์จนเสร็จสมบูรณ์

```
[1. สโคปงาน] ──> [2. เขียนสเปก] ──> [3. แตกตั๋ว] ──> [4. ดีไซน์ UI] ──> [5. โค้ดดิ้ง] ──> [6. ตรวจรับ]
```

| ขั้นตอน | คำสั่ง / สกิล | สิ่งที่ต้องทำ | ผลลัพธ์ที่ได้ |
|:---|:---|:---|:---|
| **1. สโคปงาน** | `/grill-me`<br>*(กำหนดใช้สำหรับ Todolist)* | ถาม-ตอบเค้นขอบเขตและเงื่อนไขจนนิ่ง | ขอบเขตงานที่ชัดเจน |
| **2. เขียนสเปก** | `/to-spec` | สรุปบทสนทนาทั้งหมดเป็นเอกสารทางการ | เอกสาร Spec & Decisions |
| **3. แตกตั๋วงาน** | `/to-tickets`<br>*(ตรวจแผน: `/scrutinize`)* | ซอยเป็น Vertical Tickets ตามลำดับ Block | ตั๋วงานพร้อมทำทีละใบ |
| **4. ดีไซน์ UI** *(ถ้ามี)* | `/ui-ux-pro-max`<br>`/impeccable` | ดึงโทเคนสี/ฟอนต์ + ขัดเกลารสนิยมลบความโหล | Design System & Styling |
| **5. โค้ดดิ้ง** | `/implement`<br>*(คู่กับ `/tdd`)* | วงรอบ Red-Green (เขียนเทสต์ก่อนโค้ด) | โค้ดที่ผ่านเทสต์ครบ |
| **6. ตรวจรับ** | `/code-review`<br>`/scrutinize` | ตรวจ Code Smells + ไล่ Call Path จริง | โค้ดพร้อม Merge |

---

## 🔴 PATH 2: แก้ไขบั๊ก (Bugfix & Incident)

ใช้เมื่อระบบมีข้อผิดพลาด หรือเจอรายงานบั๊ก **(ห้ามเดา / ห้ามแก้ตามอาการ)**

```
[1. หาสาเหตุแท้จริง] ───────> [2. ลงมือแก้] ───────> [3. บันทึก RCA]
    /debug-mantra               แก้ที่ต้นเหตุ              /post-mortem
```

1. **สืบสวนด้วยวินัย 4 ข้อ (`/debug-mantra`):**
   - **Step 1:** ทำ Repro ให้ติดแน่นอน 100% (ถ้าจำลองไม่ได้ = หยุด ห้ามเดา)
   - **Step 2:** แกะ Fail Path ตามรอยโค้ดและใส่ Probe Log
   - **Step 3:** ตั้งสมมติฐาน แล้วพยายามหักล้างก่อนเชื่อ
   - **Step 4:** จดบันทึกผลการทดลอง (Ledger)
2. **แก้ไขที่ Root Cause:**
   - แก้ที่ต้นเหตุแท้จริงจน Repro เดิมรันผ่าน
3. **บันทึกประวัติทางวิศวกรรม (`/post-mortem`):**
   - สรุปปัญหา, Root Cause, การทดสอบ, และวิธีป้องกันในอนาคต

---

## 🟡 PATH 3: ตรวจสอบโค้ด / รับความเห็นที่สอง (Review & Audit)

ใช้เมื่อมี PR ส่งมา, มีโค้ดอยู่แล้ว, หรือต้องการท้าทายสถาปัตยกรรม

```
[1. ท้าทายความจำเป็น & เช็ก Call Path] ───> [2. ตรวจสอบมาตรฐาน & Smells]
               /scrutinize                               /code-review
```

1. **`/scrutinize`:** สวมหมวกคนนอก ถามว่า *"จำเป็นต้องมีโค้ดนี้ไหม? มีวิธีง่ายกว่านี้ไหม?"* และไล่เส้นทางรันจริง
2. **`/code-review`:** ตรวจสอบตาม Fowler Code Smells และกฎของ Repository

---

## ⚡ ตารางสรุปด่วน (Cheat Sheet)

| ต้องการทำอะไร? | พิมพ์คำสั่งนี้ทันที |
|:---|:---|
| จะเริ่มฟีเจอร์ใหม่ แต่ยังไม่ชัดเจน | `/grill-me` |
| คุยฟีเจอร์จบแล้ว อยากได้ Spec ทางการ | `/to-spec` |
| มี Spec แล้ว อยากแตกตั๋วทำงาน | `/to-tickets` |
| อยากตรวจความสมเหตุสมผลของ Plan หรือ PR | `/scrutinize` |
| พร้อมเขียนโค้ดตามตั๋ว | `/implement` |
| โค้ดเสร็จแล้ว อยากตรวจก่อนส่ง | `/code-review` |
| เจอ Error / บั๊ก ระบบพัง | `/debug-mantra` |
| แก้บั๊กเสร็จ อยากบันทึกบทเรียน | `/post-mortem` |
